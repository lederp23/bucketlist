machine:
  python:
    version: 3.6.2
  environment:
    SECRET_KEY: "SECRET"
    repo_token: RHWqhQE4f7RScvL6p2iRqGFiXZkmoMhBq

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - createdb bucketlist
    - nosetests tests/test.py --cover-package=v1 --cover-erase
  post:
    - coveralls
