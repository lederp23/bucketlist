machine:
  python:
    version: 3.6.2
  environment:
    SECRET_KEY: "SECRET"

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - createdb bucketlist
    - nosetests tests/test.py --cover-package=v1 --cover-erase
