machine:
  python:
    version: 3.6.2
  environment:
    SECRET_KEY: "SECRET"
    repo_token: FXPIfcK7LGoEAwp7b82OBt3VsX47ksYKP

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - createdb bucketlist
    - nosetests tests/test.py --cover-package=v1 --cover-erase
  post:
    - coveralls
